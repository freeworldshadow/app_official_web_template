{
	"buildCommand": "npm run build",
	"installCommand": "npm ci || npm install",
	"outputDirectory": "dist",
	"framework": "vite",
	"functions": {
		"api/**/*.ts": {
			"runtime": "nodejs20.x"
		}
	},
	"rewrites": [
		{
			"source": "/api/(.*)",
			"destination": "/api/$1"
		}
	]
}

